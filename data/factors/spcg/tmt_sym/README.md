# tmt_sym SPCG/ILU Factors

Local-only generated factor directory. Expected local files are SuperLU `L/U`
Matrix Market factors for `tmt_sym`.

Factor `.mtx` files are intentionally ignored by Git. See
`docs/NOT_UPLOADED_TO_GITHUB.md`.
