# Matrix Inputs

Matrix Market inputs live under `real/` and `regression/`. The `.mtx` files are
local-only and ignored by Git; use the preparation scripts and manifests to
recreate them.

Matrix Market 输入位于 `real/` 和 `regression/`。`.mtx` 文件仅保留在本地，不进入 Git；
可通过准备脚本和 manifest 重建。
