# Regression Matrix Inputs

Small Matrix Market regression inputs are stored locally in this directory.
They are currently ignored by the global `*.mtx` rule for consistency.

小型 Matrix Market 回归测试输入保存在本目录。当前由于全局 `*.mtx` 规则，这些文件也不上传。
