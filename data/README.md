# Data Directory

This directory contains local matrix inputs and generated factors. Large
`.mtx` files are intentionally ignored by Git. See
`docs/NOT_UPLOADED_TO_GITHUB.md` for the full list and recreation commands.

本目录保存本地矩阵输入和生成因子。大型 `.mtx` 文件默认不上传 Git。完整说明见
`docs/NOT_UPLOADED_TO_GITHUB.md`。
